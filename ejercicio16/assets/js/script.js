/*
 * FUNCIONES FLECHA
 */

const suma = (a, b) => {
  sumas = a + b;
  document.write(sumas + "<br>");
};

suma(5, 6);
suma(7, 6);

const nombres = nombre => document.write(nombre);
nombres("Luis");
