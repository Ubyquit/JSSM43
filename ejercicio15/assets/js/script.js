/*
 * FUNCIONES POR VARIABLE
 */

const obtenerNumero = function (argumento) {
  if (argumento == 6) {
    document.write(argumento + " es igual a 6 <br> ");
  } else {
    document.write(argumento + " es diferente a 6 <br> ");
  }
};

obtenerNumero(6);
